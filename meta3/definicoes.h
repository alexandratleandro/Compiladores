#ifndef DEFINICOES
#define DEFINICOES


typedef struct auxLista* lista;
typedef struct auxNo* arvore;
typedef struct auxNo* no;

typedef struct symbol* simbolo;
typedef struct table* tabela;

typedef struct {
		char* str;
		int linhaErr;
		int colErr;
	} lexInfo;





#endif